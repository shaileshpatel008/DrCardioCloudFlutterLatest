name: drcardio_flutter
description: "Dr. Cardio ECG — Flutter/GetX rebuild of the drcardiocloudsp Android app."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.5.4

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  # State management / DI / routing (as required by the architecture brief).
  get: ^4.6.6
  get_storage: ^2.1.1

  # Bluetooth: classic SPP for Android (the ECG device's current firmware),
  # BLE for iOS (and as an Android alternative) per the BLE-capable hardware.
  # Vendored locally (third_party/flutter_bluetooth_serial): the pub.dev 0.4.0
  # release is unmaintained and its android/build.gradle still calls the
  # long-dead jcenter() repository and pins a pre-AGP-9 classpath, so it can't
  # build against a modern Android Gradle Plugin. Same Dart API, patched build.
  flutter_bluetooth_serial:
    path: third_party/flutter_bluetooth_serial
  flutter_reactive_ble: ^5.4.0
  permission_handler: ^11.3.1

  # Real-time multi-lead ECG waveform rendering.
  fl_chart: ^0.69.0

  # Networking / API sync.
  dio: ^5.7.0
  connectivity_plus: ^6.1.0

  # Local/offline persistence (recordings queue, cached report list).
  sqflite: ^2.4.1
  path_provider: ^2.1.4
  path: ^1.9.0

  # PDF report generation + share/print/export.
  pdf: ^3.11.1
  printing: ^5.13.2

  # Native PDF rendering for the in-app viewer (smooth pinch-to-zoom,
  # scales to fit) — printing's PdfPreview is a print-preview widget, not
  # built for this; pdfx renders through the platform's own PDF engine.
  pdfx: ^2.10.0

  # Onboarding walkthrough.
  smooth_page_indicator: ^1.2.0+3

  intl: ^0.19.0
  equatable: ^2.0.5
  logger: ^2.4.0
  package_info_plus: ^8.1.1
  geolocator: ^13.0.2
  share_plus: ^10.1.2
  url_launcher: ^6.3.1

  # Patient photo (camera/gallery) and signature capture.
  image_picker: ^1.2.3
  signature: ^6.4.0

  # App-wide Nunito font, fetched from Google Fonts at runtime (see
  # AppTheme) rather than bundled as a local asset.
  google_fonts: ^8.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^4.0.0

# reactive_ble_mobile (flutter_reactive_ble's native Android impl) hardcodes
# compileSdkVersion 33 in every release through 5.5.0, which fails
# checkDebugAarMetadata against modern transitive androidx deps. Vendored
# locally (third_party/reactive_ble_mobile) with just that line bumped to 35.
# It's a transitive dependency of flutter_reactive_ble, not a direct one,
# hence dependency_overrides rather than a direct dependency line.
dependency_overrides:
  reactive_ble_mobile:
    path: third_party/reactive_ble_mobile

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/images/

  # No local `fonts:` bundle — Nunito is loaded at runtime via the
  # `google_fonts` package (see the dependency above and AppTheme)
  # instead of shipping font files as assets.

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
